import 'package:flutter/material.dart';

class NatureDetails extends StatelessWidget {
  const NatureDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
